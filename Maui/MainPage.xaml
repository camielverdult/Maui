<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:DataType="maui:MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:maui="clr-namespace:Maui"
    x:Class="Maui.MainPage" BackgroundColor="#313131"
    Title="Maui">

    <StackLayout>
        <Label Text="Maui"
               VerticalOptions="Start"
               TextColor="#b0b0b0"
               FontSize="Header"
               Margin="10"
               />

        <Label Text="Websites to check availability:"
               TextColor="#b0b0b0"
               Margin="25"
               />

        <CollectionView
            ItemsSource="{Binding DataEntrys}"
            
            x:Name="Collectionview"
              >

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="maui:DataEntry">

                    <StackLayout Padding="10">
                        <Entry Text="{Binding Address}"
                               />
                        <Entry Text="{Binding Port}"
                               />
                        <Label Text="{Binding ResponseCode}"
                               />
                        <Label Text="{Binding ResponseTime}"
                               />
                        <ActivityIndicator
                            IsRunning="False"
                                />
                    </StackLayout>

                </DataTemplate>
            </CollectionView.ItemTemplate>
            
        </CollectionView>

        <Button  Text="Add"
                 FontSize="Large"
                 VerticalOptions="EndAndExpand"
                 TextColor="#b0b0b0"
                 Command="{Binding AddEntry}"
                />

    </StackLayout>

</ContentPage>